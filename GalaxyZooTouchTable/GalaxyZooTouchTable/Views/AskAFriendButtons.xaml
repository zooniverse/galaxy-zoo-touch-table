<UserControl x:Class="GalaxyZooTouchTable.Views.AskAFriendButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GalaxyZooTouchTable.Views"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="31" d:DesignWidth="287">
    <UserControl.Resources>
        <Style x:Key="BusyCircle" TargetType="{x:Type fa:ImageAwesome}">
            <Setter Property="Foreground" Value="Red"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Active}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Viewbox>
        <Border Width="287" CornerRadius="3" Height="31" Background="#191A21">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="117*"/>
                    <ColumnDefinition Width="170*"/>
                </Grid.ColumnDefinitions>

                <TextBlock
                    TextWrapping="Wrap"
                    Foreground="White"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    TextAlignment="Right"
                    Text="See what a neighbor at the table thinks about this galaxy image."
                    FontFamily="/GalaxyZooTouchTable;component/Fonts/#Karla"
                    FontWeight="Bold"
                    FontSize="7"/>

                <ListView
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    Grid.Column="1"
                    Background="Transparent"
                    BorderThickness="0"
                    ItemsSource="{Binding AskAFriend.AvailableUsers}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="3,0,-10,0">
                                <Image Source="{Binding Avatar}" Height="25"/>
                                <fa:ImageAwesome
                                    Style="{StaticResource BusyCircle}"
                                    Icon="MinusCircle"
                                    Height="10"
                                    Margin="0,0,3,0"
                                    VerticalAlignment="Bottom"
                                    HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Viewbox>
</UserControl>
